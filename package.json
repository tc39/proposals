{
  "scripts": {
    "check-markdown": "node tools/markdown-checker/index.js",
    "test": "jest --no-cache --verbose",
    "test:coverage": "jest --no-cache --verbose --coverage"
  },
  "husky": {
    "hooks": {
      "pre-commit": "jest --no-cache --verbose"
    }
  },
  "devDependencies": {
    "husky": "^3.0.0",
    "jest": "^24.8.0",
    "remark-parse": "^6.0.3",
    "unified": "^7.1.0"
  }
}
